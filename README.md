# 📚 MongoDB Mastery Assignment

This repository contains a comprehensive set of backend tasks focused on **Pure MongoDB** operations using Node.js and Express. It covers everything from basic CRUD operations to advanced Aggregation Pipelines.

## 🚀 Features & Implementation

- **Pure MongoDB Driver**: No Mongoose, focusing on native MongoDB syntax.
- **Service-Controller Architecture**: Clean separation of business logic and routing.
- **Advanced Querying**: Logical operators, regex, and type checking.
- **Aggregation Framework**: Utilizing `$match`, `$project`, `$unwind`, and `$lookup` (Joins).
